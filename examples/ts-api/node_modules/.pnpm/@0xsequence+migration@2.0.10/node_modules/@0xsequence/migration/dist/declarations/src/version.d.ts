import { commons } from '@0xsequence/core';
export declare function counterfactualVersion(address: string, firstImageHash: string, versions: commons.context.WalletContext[]): number;
export interface Version<C extends commons.config.Config, S extends commons.signature.Signature<C>, U extends commons.signature.UnrecoveredSignature> {
    version: number;
    coders: {
        config: commons.config.ConfigCoder<C>;
        signature: commons.signature.SignatureCoder<C, S, U>;
    };
}
