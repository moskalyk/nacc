import { ethers } from 'ethers';
import { EIP1193Provider, EIP1193ProviderFunc, JsonRpcSender } from "./types.js";
export declare class JsonRpcHandler implements EIP1193Provider, JsonRpcSender {
    private provider;
    private defaultChainId?;
    constructor(provider: EIP1193ProviderFunc | JsonRpcSender | ethers.JsonRpcProvider, defaultChainId?: number);
    request: (request: {
        method: string;
        params?: any[];
        chainId?: number;
    }) => Promise<any>;
    send(method: string, params?: any[], chainId?: number): Promise<any>;
}
