import { BaseProviderTransport } from "../base-provider-transport.js";
import { ProviderMessage, OpenWalletIntent } from "../../types.js";
import { ProxyMessageChannelPort } from "./proxy-message-channel.js";
export declare class ProxyMessageProvider extends BaseProviderTransport {
    private port;
    constructor(port: ProxyMessageChannelPort);
    register: () => void;
    unregister: () => void;
    openWallet: (path?: string, intent?: OpenWalletIntent, networkId?: string | number) => void;
    closeWallet(): void;
    sendMessage(message: ProviderMessage<any>): void;
}
