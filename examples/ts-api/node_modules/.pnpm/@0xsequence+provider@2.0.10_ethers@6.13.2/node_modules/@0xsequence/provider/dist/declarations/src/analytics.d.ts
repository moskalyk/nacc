import { Databeat, Event as DatabeatEvent } from '@databeat/tracker';
export declare enum EventType {
    INIT = 0,
    VIEW = 1,
    SIGN_MESSAGE_REQUEST = 2,
    SIGN_TYPED_DATA_REQUEST = 3,
    SEND_TRANSACTION_REQUEST = 4
}
export type EventTypes = keyof typeof EventType;
export type Event = DatabeatEvent<EventTypes>;
export declare class Analytics extends Databeat<EventTypes> {
}
export declare const setupAnalytics: (projectAccessKey: string, server?: string) => Analytics;
